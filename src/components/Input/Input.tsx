import React from 'react';

export const Input = ({}: any) => {
  return (
    <input
      id="client.phone"
      aria-describedby="client.phone-errors"
      disabled={undefined}
      className="ant-input ant-input-disabled"
      type="text"
      value=""
    />
  )
}